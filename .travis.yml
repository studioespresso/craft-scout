cache:
  directories:
    - $HOME/.composer/cache

language: php

services:
  - docker

matrix:
  include:
    - php: 7.1
      env: CODECEPT_FLAGS='--coverage --coverage-xml'
    - php: 7.2
      env: CODECEPT_FLAGS=''
    - php: 7.3
      env: CODECEPT_FLAGS=''
    - php: 7.4snapshot
      env: CODECEPT_FLAGS=''
  fast_finish: true
  allow_failures:
    - php: 7.4snapshot
    
before_install:
  - composer config discard-changes true
  - travis_retry composer self-update

install:
  - travis_retry composer update ${COMPOSER_FLAGS} --prefer-dist --no-interaction --no-suggest

before_script:
  - sudo service mysql stop
  - docker-compose -f ./tests/docker-compose.yml up -d
  - php tests/wait.php
  - cp ./tests/.env.travis ./tests/.env

script:
  - vendor/bin/codecept run unit ${CODECEPT_FLAGS}

after_script:
  - docker-compose -f ./tests/docker-compose.yml down